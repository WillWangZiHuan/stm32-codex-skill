interface:
  display_name: "STM32 Manual-Driven Builder"
  short_description: "Build STM32 projects from manuals or shared board data"
  default_prompt: "Use $stm32-cubemx-build to select a validated community board package or read my board manual, then generate and compile the requested STM32 project."
